spring.application.name=bikehubb

# Hibernate/JPA - Permite criar/atualizar tabelas automaticamente
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Configuração do banco de dados Supabase
# IMPORTANTE: Este projeto Supabase NÃO tem IPv4, apenas IPv6
# Usando técnicas especiais para conectar via Connection Pooler
spring.datasource.url=jdbc:postgresql://db.krlhnihkslmmihprkwqm.supabase.co:6543/postgres?sslmode=require&preferQueryMode=simple&rewriteBatchedInserts=true&connectTimeout=30&socketTimeout=60&prepareThreshold=0
spring.datasource.username=postgres
spring.datasource.password=Weboito8@Websete7@

# Driver JDBC
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Configurações específicas para IPv6 e rede
spring.jpa.properties.hibernate.connection.provider_disables_autocommit=true

# Pool de conexão otimizado para problemas de rede
spring.datasource.hikari.maximum-pool-size=2
spring.datasource.hikari.minimum-idle=1
spring.datasource.hikari.connection-timeout=60000
spring.datasource.hikari.idle-timeout=300000
spring.datasource.hikari.max-lifetime=600000
spring.datasource.hikari.leak-detection-threshold=120000
spring.datasource.hikari.initialization-fail-timeout=-1

# Configurações adicionais recomendadas
# Permitir upload de arquivos
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=50MB

# Configuracoes do Stripe
stripe.secret.key=sk_test_4eC39HqLyjWDarjtT1zdp7dc
stripe.publishable.key=pk_test_TYooMQauy1zV0N0rQ8y5K5cQ
stripe.webhook.secret=whsec_test_4eC39HqLyjWDarjtT1zdp7dc

#Configuracao do servidor (opcional)
server.port=8080